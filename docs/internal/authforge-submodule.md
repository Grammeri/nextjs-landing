# 🔗 AuthForge Submodule Workflow

В этом документе описано, **как правильно работать с сабмодулем AuthForge**
в проекте `nextjs-landing`.

Документ предназначен **только для разработчиков проекта**  
и **не должен публиковаться в браузере или маркетинговых docs**.

---

## 🧠 Как устроена связь репозиториев

### Есть два отдельных Git-репозитория:

1. **AuthForge**
   - Репозиторий: `github.com/Grammeri/AuthForge`
   - Ветка: `main`
   - Это **самостоятельный проект**
   - В нём живёт код и документация AuthForge

2. **Next.js Landing**
   - Репозиторий: `github.com/Grammeri/nextjs-landing`
   - Использует AuthForge как **git submodule**
   - Сабмодуль расположен в:
     ```
     content/authforge
     ```

---

## 🔗 Что такое git submodule в этом проекте

`content/authforge` — это **не копия файлов**, а **указатель на конкретный коммит**
репозитория AuthForge.

📌 ВАЖНО:

- лендинг **не хранит код AuthForge**
- он хранит **хэш коммита**, на который сейчас указывает сабмодуль
- обновление AuthForge = обновление указателя на новый коммит

---

## 🔁 Правильный рабочий процесс (ОБЯЗАТЕЛЬНО)

### ✅ Шаг 1. Работаем в репозитории AuthForge

```bash
cd auth-forge


Вносим изменения (docs, код, что угодно).

Проверяем статус:

git status


Коммитим изменения В AUTHFORGE:

git add .
git commit -m "docs(authforge): update development setup"
git push
```

👉 На этом этапе AuthForge полностью синхронизирован с GitHub.

✅ Шаг 2. Обновляем сабмодуль в лендинге

Переходим в проект лендинга:

```bash
cd nextjs-landing
```

Обновляем сабмодуль:

```bash
git submodule update --remote content/authforge
```

Git подтянет новый коммит AuthForge.

🔍 Проверка
```bash
git status
```

Ты увидишь:

```
modified: content/authforge (new commits)
```

👉 Это нормально и правильно.

Это означает:

☝️ указатель сабмодуля обновился и теперь ссылается на новый коммит AuthForge

✅ Шаг 3. Коммитим обновление сабмодуля
```bash
git add content/authforge
git commit -m "chore(submodule): update authforge documentation"
git push
```

🎉 Готово.

🧩 Почему в сабмодуле HEAD detached

Если зайти внутрь сабмодуля:

```bash
cd content/authforge
git status
```

Можно увидеть:

```
HEAD detached at ceb1e36
```

❗ Это НЕ ошибка

Это нормальное поведение git submodule:

сабмодуль не находится на ветке

он прикреплён к конкретному коммиту

ветки внутри сабмодуля не используются

👉 Ничего делать не нужно.

❌ Что делать НЕЛЬЗЯ

🚫 Коммитить файлы AuthForge из проекта лендинга
🚫 Делать git checkout main внутри сабмодуля
🚫 Пытаться править AuthForge без перехода в его репозиторий
🚫 Коммитить лендинг до коммита AuthForge

🧪 Как проверить, что всё синхронизировано
В AuthForge:
```bash
git status
git log -1 --oneline
```

Ожидаемо:

```
On branch main
Your branch is up to date with 'origin/main'.
```

В лендинге:
```bash
git status
```

Ожидаемо:

```
nothing to commit, working tree clean
```

🏁 Итог

AuthForge — отдельный репозиторий

Лендинг — использует указатель на коммит AuthForge

HEAD detached в сабмодуле — норма

Всегда сначала:

коммитим AuthForge

обновляем сабмодуль

коммитим лендинг

💡 Такой подход гарантирует:

отсутствие конфликтов

чистую историю git

предсказуемое поведение CI
