# 🔗 Design Tokens System (Internal)

В этом проекте используется система дизайн-токенов (design tokens) для управления
визуальным стилем и интерактивным поведением интерфейса.

Design tokens — это централизованные CSS-переменные.

---

## 🧠 Архитектура

Design tokens описывают:
- цвета
- поверхности
- тени
- motion и hover-эффекты
- интерактивное поведение

Цель системы — обеспечить:
- визуальную согласованность
- предсказуемое поведение UI
- масштабируемость
- безопасные изменения без побочных эффектов

Система токенов разделена на несколько логических слоёв.

### 1. Primitive tokens

Базовые, атомарные значения.
Не используются напрямую в компонентах.

Примеры:
```css
--color-red-500
--opacity-90
--scale-sm
```

### 2. Semantic tokens

Семантические токены описывают назначение значения,
а не его физическую форму.

Используются в компонентах.

Примеры:
- `--color-primary`
- `--surface-card`
- `--text-secondary`

### 3. Interaction tokens

Токены интерактивного поведения.
Описывают hover, focus, active, motion.

Примеры:
- `--interaction-hover-opacity`
- `--interaction-hover-scale`
- `--interaction-hover-translate-y`

### 4. Alias tokens

Alias-токены связывают semantic и interaction токены с primitive значениями.

Они позволяют менять поведение системы,
не трогая компоненты.

---

## 🔁 Workflow

Как добавлять новый токен (чеклист):

Определи тип:
- primitive
- semantic
- interaction

Проверь:
- используется ли похожий токен
- нужен ли новый слой

Добавь токен:
- в `globals.css`
- с понятным неймингом

Используй токен в компонентах,
а не raw значение.

---

## 🧠 Edge cases

### Interaction / Hover tokens

Интерактивные токены существуют для того,
чтобы в проекте не было магических чисел в hover-стилях.

❌ Плохой пример
```css
.button:hover {
  transform: scale(1.03);
  opacity: 0.9;
}
```

Проблемы:
- значение не переиспользуемо
- сложно изменить глобально
- нет системного смысла

✅ Хороший пример
```css
.button:hover {
  transform: scale(var(--interaction-hover-scale));
  opacity: var(--interaction-hover-opacity);
}
```

Преимущества:
- единое поведение
- централизованное управление
- безопасные изменения

---

## 📌 Правила и ограничения

❌ Запрещено:
- использовать raw значения в UI
- добавлять случайные hover-числа
- дублировать поведение между компонентами

✅ Разрешено:
- использовать только semantic и interaction токены
- добавлять новые токены через систему
- переопределять поведение через alias

---

## 🧪 Проверка

CI и поддержка системы

Система токенов является частью архитектуры проекта.

Любые изменения должны:
- не ломать существующий UI
- быть обратимо управляемыми
- соответствовать принципам системы

---

## 🏁 Итог

Design tokens — это фундамент визуальной архитектуры проекта.

Они:
- защищают UI от хаоса
- ускоряют разработку
- делают изменения безопасными

Если кажется, что токен не нужен — скорее всего, он нужен.
