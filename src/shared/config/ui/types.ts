export type UiText = {
  payment: {
    single: string;
    multiple: string;
  };
  pricing: {
    deliveryNotice: string;
    termsPrefix: string;
    termsLabel: string;
    termsError: string;
    footerNote: string;
  };
};
