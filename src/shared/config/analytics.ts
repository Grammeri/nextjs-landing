/**
 * Site analytics configuration.
 *
 * Custom event helpers can be added later (e.g. AuthForge checkout start,
 * Starter free download) via @next/third-parties sendGAEvent, gated by
 * analytics consent from `@/shared/lib/analytics/consent`.
 */
export const GA_MEASUREMENT_ID = 'G-J732TGJ2BT' as const;
