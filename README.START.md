# 🔴 ⛔ КРИТИЧЕСКИ ВАЖНО: Этот файл должен быть удалён перед отправкой

**Файл `README.START.md` является служебным и НЕ должен присутствовать в финальной версии проекта, отправляемой работодателю.**

Наличие этого файла в финальном репозитории считается ошибкой оформления тестового задания.

## Что нужно сделать обязательно:

1. **Сохраните инструкции** — скопируйте содержимое этого файла в ваши заметки (Word, Notion, Google Docs или другой редактор)
2. **Удалите файл** — полностью удалите `README.START.md` из репозитория перед отправкой задания
3. **Очистите git-историю** — создайте чистый репозиторий с одним начальным коммитом (инструкции ниже)

Этот файл содержит внутренние инструкции для подготовки проекта и предназначен только для работы над заданием. После сохранения инструкций в личных заметках файл должен быть удалён.

---

# Инструкция по подготовке проекта

## Что это за проект

Это проект на Next.js с базовой конфигурацией TypeScript и инструментов разработки. Проект используется как основа для выполнения тестовых заданий.

Все базовые настройки уже выполнены: настроены TypeScript, ESLint, Prettier и базовые инструменты контроля качества кода. Остаётся только установить зависимости и начать работу над заданием.

## Установка и запуск

Установите зависимости. Рекомендуется использовать pnpm:

```bash
pnpm install
```

Если pnpm не установлен, можно использовать npm:

```bash
npm install
```

После установки зависимостей запустите dev-сервер:

```bash
pnpm dev
```

или

```bash
npm run dev
```

Проект будет доступен по адресу http://localhost:3000

## Структура проекта

Основной код приложения находится в папке `src/app/`. Здесь располагаются:

- `page.tsx` — главная страница приложения
- `layout.tsx` — корневой layout
- `globals.css` — глобальные стили

Статические файлы (изображения, иконки) размещаются в папке `public/`.

Конфигурационные файлы:

- `next.config.ts` — настройки Next.js
- `tsconfig.json` — настройки TypeScript
- `eslint.config.mjs` — настройки ESLint
- `.prettierrc` — настройки Prettier
- `package.json` — зависимости и скрипты

## Доступные команды

- `pnpm dev` или `npm run dev` — запуск dev-сервера
- `pnpm build` или `npm run build` — сборка production-версии
- `pnpm start` или `npm run start` — запуск production-сервера (после сборки)
- `pnpm lint` или `npm run lint` — проверка кода с помощью ESLint
- `pnpm format` или `npm run format` — форматирование кода с помощью Prettier

## Формат сообщений коммитов

В проекте включена автоматическая проверка сообщений коммитов. При создании коммита система проверит, соответствует ли ваше сообщение установленному формату.

Коммиты должны соответствовать стандарту [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/). Это помогает поддерживать понятную и структурированную историю изменений.

### Шаблон сообщения коммита

```
type: short description
```

### Примеры правильных коммитов

```
feat: add user login page
fix: handle empty state
docs: update README
chore: update dependencies
refactor: simplify component logic
test: add unit tests
```

### Примеры неправильных коммитов

Такие коммиты будут отклонены проверкой:

```
update
fix
wip
my changes
123
test commit
```

При использовании правильного формата ваши коммиты будут автоматически проверяться, и если формат не соответствует требованиям, коммит не будет создан. Это помогает поддерживать качество истории изменений в проекте.

Подробнее о стандарте Conventional Commits можно прочитать на официальном сайте: https://www.conventionalcommits.org/en/v1.0.0/

## Что сделать перед отправкой тестового задания

Перед отправкой тестового задания выполните следующие действия:

1. **Удалите этот файл README.START.md**

2. **Очистите git-историю и создайте чистый репозиторий**

Выполните команды:

```bash
rm -rf .git
git init
git add .
git commit -m "Initial commit"
```

Если вы работаете на Windows и команда `rm -rf .git` недоступна, используйте:

```bash
rmdir /s /q .git
git init
git add .
git commit -m "Initial commit"
```

3. **Проверьте, что проект собирается без ошибок**

```bash
pnpm build
pnpm lint
```

4. **Создайте репозиторий и загрузите код**

Создайте новый репозиторий на GitHub или другой платформе и отправьте туда ваш код.

---

## Чеклист перед отправкой тестового задания

Перед отправкой тестового задания убедитесь, что выполнены все пункты:

- [ ] Файл `README.START.md` удалён из репозитория
- [ ] Git-история очищена, создан чистый репозиторий с одним начальным коммитом
- [ ] Проект успешно собирается без ошибок (`pnpm build`)
- [ ] Линтер проходит без ошибок (`pnpm lint`)

**Помните: этот файл README.START.md необходимо удалить перед отправкой тестового задания.**
